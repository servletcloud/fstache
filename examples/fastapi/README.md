# Fstache FastAPI Example

This is a standalone `uv` project that installs Fstache from PyPI.

```bash
cd examples/fastapi
uv sync
uv run uvicorn app:app --reload
```

Then visit:

- http://127.0.0.1:8000/
- http://127.0.0.1:8000/stream
