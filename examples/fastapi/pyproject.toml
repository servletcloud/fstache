[project]
name = "fstache-fastapi-example"
version = "0.1.0"
description = "Small FastAPI example using Fstache templates"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "fstache>=0.1.3",
    "uvicorn>=0.51.0",
]
