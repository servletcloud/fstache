[project]
name = "fstache-starlette-example"
version = "0.1.0"
description = "Small Starlette example using Fstache templates"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fstache>=0.1.3",
    "starlette>=1.3.1",
    "uvicorn>=0.49.0",
]
