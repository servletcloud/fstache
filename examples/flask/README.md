# Fstache Flask Example

This is a standalone `uv` project that installs Fstache from PyPI.

```bash
cd examples/flask
uv sync
uv run flask --app app run --debug
```

Then visit:

- http://127.0.0.1:5000/
- http://127.0.0.1:5000/stream
